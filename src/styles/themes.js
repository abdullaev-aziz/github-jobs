import colors from "./colors";
const lightTheme = {
  backgroundColor: colors.lightGray,
  color: colors.black,
};

const darkTheme = {
  backgroundColor: colors.darkBlue,
  color: colors.white,
};

export default {
  lightTheme,
  darkTheme,
};
