import colors from "./colors";
const lightTheme = {
  App: {
    backgroundColor: colors.lightGray,
    color: colors.black,
  },
};

const darkTheme = {
  App: {
    backgroundColor: colors.darkBlue,
    color: colors.white,
  },
};

export default {
  lightTheme,
  darkTheme,
};
